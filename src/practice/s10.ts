// 🧩 Задача 10 — Отчёт: генерация, рендер, экспорт
// ts
// Копировать код
// class SalesReport {
//   buildData(from: Date, to: Date) {
//     // достаём продажи, агрегируем
//   }
//
//   toHtml() {
//     // строим html-разметку
//   }
//
//   toCsv() {
//     // строим csv
//   }
//
//   download(filename: string) {
//     // инициируем скачивание файла в браузере
//   }
// }
// Твоя задача:
// Укажи:
//
// Какие тут разные зоны ответственности?
//
// На какие 2–3 класса/сервиса ты бы это разбил, и кто за что отвечал бы?


// Ответ:
// В данном классе можно выделить следующие зоны ответственности:
// 1. Сбор и агрегация данных о продажах (buildData).
// 2. Генерация различных форматов отчёта (toHtml, toCsv).
// 3. Экспорт и скачивание отчёта (download).
// Для разделения этих зон ответственности можно создать следующие классы/сервисы:
// 1. SalesDataService:
//   - Ответственность: Сбор и агрегация данных о продажах.
//   - Метод: buildData(from: Date, to: Date).
//   2. ReportGenerator:
//   - Ответственность: Генерация отчётов в различных форматах.
//   - Методы: toHtml(data: any), toCsv(data: any).
//   3. ReportExporter:
//   - Ответственность: Экспорт и скачивание отчётов.
//   - Метод: download(content: string, filename: string).
//   Таким образом, каждый класс будет иметь одну чёткую ответственность, что улучшит читаемость, поддержку и тестируемость кода.
class SalesDataService {
  buildData(from: Date, to: Date) {
    // достаём продажи, агрегируем
  }
}
class ReportGenerator {
  toHtml(data: any) {
    // строим html-разметку
  }

  toCsv(data: any) {
    // строим csv
  }
}
class ReportExporter {
  download(content: string, filename: string) {
    // инициируем скачивание файла в браузере
  }
}
