// ===============================
// 10 ЗАДАЧ НА ДЕКОРАТОР (простые)
// ===============================

// 1) Создай базовый класс Calculator с методом calc(n)
//    Сделай декоратор DoubleDecorator → умножает результат calc на 2.
//    Сделай декоратор SquareDecorator → возводит результат calc в квадрат.

// 2) Есть класс MessageService.send(text).
//    Создай декоратор EmojiDecorator → добавляет 🙂 в конец сообщения.

// 3) Есть Logger.log(msg).
//    Создай TimestampDecorator, который добавляет дату перед сообщением.
//    Пример: [2025-11-20] msg

// 4) Есть FileWriter.write(text).
//    Добавь декоратор EncryptDecorator → переворачивает строку (text.split("").reverse())

// 5) Есть Notifier.notify(msg).
//    Создай декоратор LimitLengthDecorator → если > 10 символов, обрезать и добавить "..."

// 6) Есть Greeter.greet(name) → "Hello, name".
//    Добавь декоратор UppercaseNameDecorator → делает имя uppercase.

// 7) Есть class MathService.calc(a, b) → a + b.
//    Создай декоратор AbsDecorator → делает результат положительным (Math.abs).

// 8) Есть Api.fetch(url) → возвращает строку.
//    Создай CacheDecorator → если этот url уже вызван, вернуть "FROM CACHE".

// 9) Есть Saver.save(obj).
//    Сделай JsonDecorator → превращает obj в JSON перед сохранением.

// 10) Есть TextEditor.write(text).
//     Сделай декоратор NoDigitsDecorator → удаляет все цифры из строки перед записью.
//     Пример: "h3ll0" → "hll"

// Хочешь — сделаю решения для всех 10 задач? Или хочешь попробовать сам?
